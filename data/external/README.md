# External Data

외부에서 받은 대용량 데이터셋을 임시로 참조하는 폴더입니다.

## 규칙

- AI Hub 원본 이미지, 압축 파일, PDF/HWP 원본, 모델 weight는 Git에 커밋하지 않습니다.
- 실제 파일은 외부 저장소에 두고 `data/catalog/collection-log.md` 또는 별도 manifest에 경로만 기록합니다.
